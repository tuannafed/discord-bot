# Tracks Registry

| Status | ID | Title | Type | Phase | Created | Updated |
|--------|----|-------|------|-------|---------|---------|

<!--
Status symbols:
  [ ] pending    — BA spec not yet written
  [~] in-progress — at least one agent has worked on it
  [x] done       — code-reviewer has approved

Track types:
  feature  — new functionality (full pipeline: BA → DB → Backend → Frontend → Review)
  bug      — fix existing issue (no API Contract or DB schema needed unless relevant)
  chore    — maintenance, config, dependencies (BA → Backend or Frontend → Review)
  refactor — code improvement, no behavior change (BA → Backend/Frontend → Review)

To add a track (done by ba-agent):
  | [ ] | track-001-auth | User Authentication | feature | ba | 2025-01-15 | 2025-01-15 |

To update status (done by each agent on completion):
  - Replace [ ] with [~] when first agent starts
  - Replace [~] with [x] when code-reviewer approves
  - Update Phase and Updated columns
-->
